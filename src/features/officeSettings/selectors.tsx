export type genericSetting = {
    id: string,
}

export type settingGroup = {
    id: number,
    shortValue: string,
    longValue: string,
    description: string,
    order: number,
    icon: any,
}

